import { Placeholder } from '@/components/ui/PageChrome'

export default function OpenSlotsPage() {
  return (
    <Placeholder
      eyebrow="The people"
      title="Open slots"
      version="v0.1.7"
      description="Unbooked time across every cluster for the next two weeks — where extra capacity lives."
    />
  )
}
